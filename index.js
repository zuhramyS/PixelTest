// Define a helper function to calculate the area of a square
function calculateSquareArea(length) {
    return length * length;
}

// Example usage of the helper function
const length = 5;
const area = calculateSquareArea(length);
console.log("The area of the square is:", area);
